#include <stdio.h> 
#include "search.h"
  
// Utility function to swap two elements
void swap(int *xp, int *yp) {
    int temp = *xp;
    *xp = *yp;
    *yp = temp;
}


  
// No main function as it's been separated for unit testing purposes.
